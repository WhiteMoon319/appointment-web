{
  "name": "reminder-worker",
  "version": "1.0.0",
  "private": true,
  "description": "师生预约定时提醒 Worker：每分钟扫描到点预约并发送 OneBot 通知",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
